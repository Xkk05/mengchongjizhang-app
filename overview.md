# 随心宠记账 —— 项目总览

萌宠养成记账 App，双端（Android + iOS），Flutter 单代码库。

---

## 当前交付物

| 交付物 | 位置 | 状态 |
|---|---|---|
| **Flutter App 工程** | `app/` | ✅ 可编译可跑，v0.9 状态化动作里程碑 |
| Flutter 交付说明 | `flutter-app.md` | ✅ 含架构、验证结果、运行方式、下一步 |
| HTML 交互原型 | `prototype/index.html`（工作版）、`prototype/demo.html`（演示版） | ✅ v0.4 已完成 |

桌面：`C:\Users\admin\Desktop\随心宠记账-原型演示.html`（原型演示版，v0.4 含深色模式）。

---

## 一句话进展

- **原型阶段（已完成）**：11 屏高保真 HTML 原型，走完 v0.1 薄荷绿工具版 → v0.2 对齐 iOS 实拍重构 → v0.3 UI 精修（对比度 / 线性图标 / 控件底色）→ v0.4 设计令牌收敛 + 深色模式。
- **工程阶段（进行中）**：Flutter 工程已落地。v0.1 打通设计令牌 → Drift 本地库 → Riverpod 状态层 → 导航外壳 → 场景主页 + 记账核心；v0.2 补齐**统计页**（分类环形图 + 排行 + 近 7 日柱状图）；v0.3 补齐**资产页**（余额派生 + 账户增删改）与**预算页**（总预算 + 分类预算 + 超支提醒）；v0.4 补齐**养成闭环**——签到（7 日周期 + 连击）、商店（金币换食物/装扮）、背包（食用 + 装备），金币有了真正的消耗出口；v0.5 让**装扮真正穿到宠物身上**——头饰 / 项圈 / 场景皮肤都能看见效果；v0.6 让**宠物活起来**——体型随等级长大、待机时呼吸/眨眼/摆尾，饿了或心情差还会换表情；v0.7 补上**账单的进出通道**——CSV 导出可分享给 Excel，导入带预览、自动去重、异常行标行号；v0.8 补上**账单日历**——按月铺开每天的收支，点某天看当天明细，月份与统计页共享；v0.9 让**宠物有状态化动作**——饿了揉肚子冒咕噜、深夜闭眼打哈欠飘 Zzz、吃饱眯眼哼歌飘音符，判定收在纯函数、演出用低频独立控制器。其后又对**核心页做了 UI 精致化**——字号放大、圆角更圆、背景与文字中性化（保留珊瑚红+薄荷绿品牌），卡片改柔和阴影抬升层次；并修掉字号放大后暴露的**主页场景重叠**（第三排悬浮按钮压到余额卡，根源是场景容器高度写死 + 绝对定位浮层，已加高容器并重排悬浮列）。

---

## Flutter 工程要点

- **技术栈**：Flutter 3.44.1 / Dart 3.12.1、Riverpod 3.4.3、Drift 2.35.0、go_router 17.5.0、fl_chart 1.2.0。
- **分层**：`ui → state → data/domain`，`core`（令牌/主题/工具）被单向依赖；UI 不直接碰数据库。
- **数据模型**：账本 / 账户 / 分类 / 流水 / 宠物状态 / 预算 / 商品目录 / 背包 / 签到 九张表。金额一律存「分」（int），分类图标存 key 字符串。
- **账户余额是派生值**：账户只存建档初始余额，当前余额 = 初始余额 + 该账户流水净额，记账改账删账天然一致。
- **记账与养成联动**：写流水与结算宠物经验金币在同一事务内完成，编辑账目不重复发奖励。
- **养成闭环**：商品目录用稳定 `code` 做锚点（改名字/调价不影响签到奖励）；购买、食用、签到都在事务里结算，不会出现「扣了钱没拿到货」。
- **装扮按佩戴位互斥**：头饰 / 颈部 / 场景三档，同档只留一件、跨档可叠加；绘制层只认枚举，`code → 枚举`的映射收在领域层，加商品不用改绘制代码。
- **视觉**：宠物、场景、装扮全部用 `CustomPainter` 代码绘制，零图片资源；浅色/深色两套令牌同源。
- **宠物会成长也会动**：体型按等级平滑变化，且以**脚为锚点**向上长，不会在升级时整体下沉；呼吸 / 眨眼 / 摆尾三种待机动作由**同一个动画控制器**派生相位，天然同步不会打架；饱食与心情决定表情（开心眯眼上扬 / 低落缩眼下垂），判定收在纯函数里，绘制层只负责画。
- **状态化动作**：在通用待机之上叠加触发式演出——饿（饱食<25）揉肚子、深夜（22 点~6 点）打哈欠、吃饱心情好（≥70 且 ≥60）哼歌；判定是 `actionFor(mood,hunger,hour)` 纯函数（饿 > 困 > 满足 > 待机），演出相位用低频独立控制器，不干扰呼吸/眨眼/摆尾节奏；「我的」页静态预览也会按状态定格。
- **账单进出有据可查**：导出写成带 BOM 的 CSV（Excel 不乱码），走系统分享或剪贴板；导入**先预览再落库**，把「能导入几笔 / 跳过几笔重复 / 哪几行认不出来 / 会新建哪些分类账户」都摊开确认。去重按「指纹计数」抵消，重复导入同一文件不会产生副本，真实的同分钟多笔也不会被误伤；认不出的行跳过并标行号，不连累好数据。导入的历史账单**不发养成奖励**，避免一次导入把等级刷满。
- **日历与统计同源**：日历网格、每日收支聚合都是纯函数（日期算术最容易在闰月、跨年、1 号是周几上出错，所以单独挂测试）；月份沿用统计页的全局「当前月份」，翻月互相同步，两边口径不会打架。日历格宽度只有屏宽 1/7，金额分级降精度（100 元取整、1000 元以上取 k），避免溢出。
- **统计 / 预算共享「当前月份」**：翻月互相同步，支出口径来自同一套聚合函数。

**验证结果**：`flutter analyze` 零问题；`flutter test` **226 项全通过**（含 19 项养成闭环 + 15 项账单导入导出的内存真库端到端测试）；Drift 代码生成通过（含 v1→v4 迁移）；`flutter build apk --debug` 出包；`flutter build apk --release` 出 60.6MB 安装包（含重叠修复，已放桌面 `随心宠记账-v0.9.apk` 供自测）。

---

## 下一步

1. ~~统计页（接入 fl_chart）~~ ✅ 已完成
2. ~~资产 / 预算页~~ ✅ 已完成
3. ~~养成闭环补全：签到、商店、背包（金币消耗出口）~~ ✅ 已完成
4. ~~装扮落到宠物身上~~ ✅ 已完成
5. ~~数据层加固：CSV 导入导出~~ ✅ 已完成
6. ~~宠物形象成长：按阶段换体型 / 加待机小动作（吃饭、睡觉），让等级更有存在感~~ ✅ 已完成
7. ~~账单日历：按月看每天的收支（原型里还没迁的一屏）~~ ✅ 已完成
8. ~~正式发版前关闭演示种子数据~~ ✅ 已完成（`_seedSample=false`，全新安装为空账本；已装设备需卸载重装清掉旧演示数据）
9. 第三方账单适配：支付宝 / 微信账单各有怪格式，可加一层「来源适配器」（当前认通用表头别名）
10. ~~宠物待机的下一步：进食 / 睡眠等**状态化动作**~~ ✅ 已完成（饿揉肚子 / 困打哈欠 / 满足哼歌）
11. ~~记账面板重做~~ ✅ 已完成（保存键常驻可见、分类格放大、收入薄荷色、键盘弹起不再顶飞面板）

> 详细说明见 `flutter-app.md`；原型阶段的改动见下方历史记录。

---

# 附：HTML 原型 v0.4 记录（上一阶段）

## 做了什么
- **几何令牌化**：圆角 88 处收敛到 5 档，字号原 29 档合并到 8 档。
- **表面色令牌化**：卡片底、页面底、渐变底抽成变量 —— 同时成为深色模式的开关。
- **深色模式**：新增 `[data-theme="dark"]` 令牌组 + 右上角一键切换；深色下逐色校验 16 组对比度全部 PASS-AA。
- **v0.3 基础**：修掉浅灰小字（2.3:1）与彩底白字（2.1:1）三类不合格对比度；85 处 emoji + 16 处几何符号换成 Lucide 线性图标 sprite；彩色控件底色统一加深。

## 已知可继续做的
- 深色场景当前是 CSS 滤镜黄昏化的占位做法，正式版建议另绘夜景素材。
- 「我的」页三张功能卡白字压在橙/绿渐变上约 3:1，大字号达标、小字差一点。
